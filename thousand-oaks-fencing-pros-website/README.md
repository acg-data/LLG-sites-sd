# Thousand Oaks Fencing Pros — HTML redesign

Open `index.html` directly, or run `npm run build` to create the production copy in `dist/`.

The site is self-contained and uses only local routes. It does not link to the original domain.
