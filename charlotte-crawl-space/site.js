const menuButton=document.querySelector('#hamburger-btn');const nav=document.querySelector('#site-nav');const dropdown=document.querySelector('.dropdown');menuButton?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.classList.toggle('open',open);menuButton.setAttribute('aria-expanded',String(open))});dropdown?.querySelector('.dropdown-toggle')?.addEventListener('click',event=>{if(innerWidth<=768){event.preventDefault();dropdown.classList.toggle('open')}});document.querySelectorAll('a[href^="#"]').forEach(link=>link.addEventListener('click',()=>{nav?.classList.remove('open');menuButton?.classList.remove('open');menuButton?.setAttribute('aria-expanded','false')}));
const form=document.querySelector('#estimate-form');form?.addEventListener('submit',async event=>{event.preventDefault();const button=form.querySelector('button');const status=form.querySelector('.form-status');button.disabled=true;button.textContent='Sending…';status.textContent='';const fields=Object.fromEntries(new FormData(form).entries());const payload={siteKey:'charlotte-crawl-space',firstName:fields.firstName,lastName:fields.lastName,phone:fields.phone,email:fields.email,zipCode:fields.zipCode,service:fields.service,message:fields.message||'',consent:Boolean(fields.consent),source:location.href};try{const response=await fetch('https://launch-lead-gen-integrations-staging.justin-b75.workers.dev/v1/website-leads',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(!response.ok)throw new Error('Request failed');status.textContent='Thank you. Your request has been received.';form.reset()}catch(error){status.textContent='We could not send the form. Please call 704-288-4170.'}finally{button.disabled=false;button.textContent='Request Service'}});
